services:
  app:
    image: kc_purchases:latest
    container_name: kc_p
    ports:
      - "6005:6005"
    env_file:
      - .env.production
    environment:
      - HOSTNAME=0.0.0.0
      - PORT=6005
    restart: unless-stopped
